# Benchmarks

Benchmarks are intentionally deferred until MiniKV has real storage operations
and measurable workloads. Keeping this directory as documentation makes that
boundary explicit without introducing a benchmarking dependency prematurely.
